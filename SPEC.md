# Shift Share — サービス仕様書

## 1. サービス概要

管理者と従業員が協力してシフトを作成するウェブサービス。従業員は希望勤務時間と希望日時を申告し、管理者はそれを参考にしながらシフトを確定する。「各従業員が希望に対してどれだけ割り当てられたか（乖離）」が全員で均等になるよう調整しやすくすることを目的とする。

管理は**年度単位**（4/1〜翌3/31）で行う。

---

## 2. ユーザーロールとアクセス方式

ログイン・登録は不要。URLでロールを区別する。

| ロール | URL | 説明 |
|---|---|---|
| **管理者 (Admin)** | `/admin/*` | シフト枠の作成・確定、全体調整を行う |
| **従業員 (Worker)** | `/` | 名前をリストから選んで操作する |

### 従業員の識別方法

1. トップページ（`/`）にアクセスすると、管理者が登録した従業員名の一覧が表示される
2. 自分の名前を選ぶと従業員ダッシュボードに入る
3. セッションは保持しない（ブラウザを閉じると名前選択に戻る）

### 管理者アクセス

- `/admin` のURLを知っている人が管理者として操作できる
- パスワードなし（URLが事実上のアクセスキー）
- 将来的に認証を追加する際のスコープ外

---

## 3. 年度管理

### 年度の定義

| 項目 | 内容 |
|---|---|
| 開始日 | 4月1日 |
| 終了日 | 翌年3月31日 |
| 表記例 | 2025年度 → 2025/4/1〜2026/3/31 |

### 年度の切り替え

- 管理者が年度を作成する
- 画面上部の年度セレクターで切り替え
- 年度をまたいでのデータ参照は行わない（各年度は独立）

---

## 4. 公平性の定義

本サービスにおける「公平」とは、**絶対的な勤務時間が等しいこと**ではなく、**各従業員の希望勤務時間に対する乖離が均等であること**を意味する。

### 指標

```
乖離 (h)  = 割当勤務時間 − 希望勤務時間
乖離率 (%) = 乖離 ÷ 希望勤務時間 × 100
```

### 例

| 従業員 | 希望時間 | 割当時間 | 乖離 | 乖離率 |
|---|---|---|---|---|
| 田中 | 20h | 18h | -2h | -10% |
| 鈴木 | 40h | 38h | -2h | -5% |
| 佐藤 | 30h | 25h | -5h | -17% ← 不公平 |

全員の乖離の**標準偏差**を公平性スコアとして算出し、ゼロに近いほど公平とみなす。

---

## 5. 機能一覧

### 5.1 管理者機能

#### 営業日設定
- 年度ごとに「営業する曜日」を設定（例: 月〜土）
- 特定日を休業日として追加（祝日・臨時休業など）
- 休業日はシフト枠の自動生成から除外される

#### シフトテンプレート（繰り返しパターン）
- 曜日・開始時刻・終了時刻・必要人数をセットとして定義
- 例: 「月・水・金 / 9:00〜17:00 / 2名」
- テンプレートを年度に適用すると、営業日に該当する全日付のシフト枠が自動生成される
- 複数テンプレートを組み合わせ可能（例: 平日パターン + 土曜パターン）
- 生成後の個別シフト枠は手動で追加・削除・編集可能

#### シフト枠管理
- シフト枠の個別作成（日付・開始時刻・終了時刻・必要人数）
- シフト枠の編集・削除（確定前のみ）
- シフト枠の一覧表示（カレンダー形式・リスト形式）

#### 割当・確定
- 希望提出一覧の閲覧
- 手動での割当（従業員をシフト枠にアサイン）
- 割当確定（確定後は従業員に通知）
- 確定後の修正（変更通知を送信）

#### 従業員管理
- 従業員名の追加・削除（名前のみ、パスワード不要）
- 月間希望勤務時間の代理設定

### 5.2 従業員機能

#### 希望提出
- 月間の希望勤務時間（合計時間数）を設定
- 希望する日時・不可能な日時をカレンダー上で申告
- 申告期限内であれば変更可能

#### シフト応募
- オープンなシフト枠（募集中）に直接エントリー
- エントリーの取り消し（確定前のみ）

#### 自分のシフト確認
- 確定済みシフトの一覧
- 当月の割当時間・希望時間・乖離の確認

### 5.3 ダッシュボード（管理者・従業員両方が閲覧可）

- 年度・月の切り替えセレクターを画面上部に表示
- 全従業員の希望時間・割当時間・乖離・乖離率を一覧表示
- 乖離が大きい従業員をハイライト（閾値: 乖離率 ±20% 以上）
- 全体の公平性スコア（乖離の標準偏差）を表示
- 管理者がシフトを変更するたびにリアルタイム更新

---

## 6. データモデル

### fiscal_years（年度）

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| year | INTEGER | 年度（例: 2025 → 2025/4/1〜2026/3/31） |
| created_at | TIMESTAMP | |

### business_day_rules（営業曜日設定）

年度ごとに「何曜日を営業日とするか」を定義する。

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| fiscal_year_id | UUID | fiscal_years.id |
| day_of_week | INTEGER | 0=日, 1=月, 2=火, 3=水, 4=木, 5=金, 6=土 |

### closed_dates（休業日）

祝日・臨時休業など、営業曜日でも休む特定日を登録する。

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| fiscal_year_id | UUID | fiscal_years.id |
| date | DATE | 休業日 |
| name | TEXT | 名称（例: 元日、夏季休業） |

### shift_templates（シフトテンプレート）

繰り返しパターンを定義する。年度に適用すると shift_slots が一括生成される。

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| fiscal_year_id | UUID | fiscal_years.id |
| day_of_week | INTEGER | 0=日〜6=土 |
| start_time | TIME | 開始時刻 |
| end_time | TIME | 終了時刻 |
| required_count | INTEGER | 必要人数 |

### workers（従業員）

認証情報は持たない。名前で識別する。

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| name | TEXT | 氏名（一意） |
| desired_hours_per_month | INTEGER | 月間希望勤務時間（分単位） |
| created_at | TIMESTAMP | |

### shift_slots（シフト枠）

テンプレートから自動生成、または個別作成。

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| fiscal_year_id | UUID | fiscal_years.id |
| template_id | UUID | 自動生成元テンプレート（手動作成時は NULL） |
| date | DATE | 日付 |
| start_time | TIME | 開始時刻 |
| end_time | TIME | 終了時刻 |
| required_count | INTEGER | 必要人数 |
| status | ENUM | `open` / `confirmed` |
| created_at | TIMESTAMP | |

### availabilities（希望提出）

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| fiscal_year_id | UUID | fiscal_years.id |
| worker_id | UUID | workers.id |
| date | DATE | 対象日 |
| start_time | TIME | 希望開始時刻 |
| end_time | TIME | 希望終了時刻 |
| preference | ENUM | `preferred` / `available` / `unavailable` |
| created_at | TIMESTAMP | |

### shift_assignments（割当）

| カラム | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| shift_slot_id | UUID | shift_slots.id |
| worker_id | UUID | workers.id |
| source | ENUM | `applied`（本人応募）/ `assigned`（管理者割当） |
| status | ENUM | `tentative` / `confirmed` |
| created_at | TIMESTAMP | |

---

## 7. 画面一覧

### 従業員（認証なし）
- `/` — 名前選択（従業員リストから選ぶ）
- `/worker/[id]` — 自分のシフトカレンダー（オープン枠・確定済みを表示）
- `/worker/[id]/availability` — 希望日時提出フォーム
- `/worker/[id]/settings` — 希望月間勤務時間の設定
- `/dashboard` — ダッシュボード（全員閲覧可・公平性スコア・乖離一覧）

### 管理者（URLのみで区別）
- `/admin` — 管理者ダッシュボード（= `/dashboard` の管理者版、割当操作付き）
- `/admin/fiscal-years` — 年度一覧・作成
- `/admin/fiscal-years/[year]/business-days` — 営業曜日・休業日設定
- `/admin/fiscal-years/[year]/templates` — シフトテンプレート管理・年度展開
- `/admin/shifts` — シフト枠一覧（カレンダー）
- `/admin/shifts/[id]` — シフト枠詳細・割当管理
- `/admin/workers` — 従業員一覧・追加・削除

---

## 8. 通知

| トリガー | 送信先 | 内容 |
|---|---|---|
| シフト確定 | 該当従業員 | 確定したシフトの日時 |
| 確定済みシフトの変更 | 該当従業員 | 変更内容 |
| 希望提出期限3日前 | 全従業員 | 提出を促すリマインダー |

通知手段: アプリ内通知（MVP）。メール通知は将来対応。

---

## 9. 技術スタック

| 領域 | 採用技術 |
|---|---|
| フレームワーク | Next.js 15 (App Router) + TypeScript |
| UI | Tailwind CSS + shadcn/ui |
| ORM | Drizzle ORM |
| データベース | PostgreSQL (Neon または Supabase) |
| バリデーション | Zod |

---

## 10. スコープ外（将来対応）

- 認証（ログイン・パスワード管理）
- シフトの自動最適化（LP・ヒューリスティクス）
- メール・プッシュ通知
- モバイルアプリ
- 複数店舗・部署の管理
- 給与計算連携
